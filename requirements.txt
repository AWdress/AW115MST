# 锁定 p115client 的配套依赖，避免无缓存构建混装不兼容的新版本。
-c p115-constraints.txt

# Core dependencies (CLI required)
p115client==0.0.9.3.7   # 锁定版本：不锁会在重建镜像时被动升级，新版曾移除 __init__ 的 check_for_relogin 参数导致启动崩溃
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.66.0
colorama>=0.4.6
watchdog>=3.0.0
python-telegram-bot>=20.0
psutil>=5.9.0
ruamel.yaml>=0.17.0

# GUI dependencies (optional, local use only)
# PyQt6>=6.6.0
# PyQt6-Qt6>=6.6.0
