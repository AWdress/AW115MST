# Core dependencies (CLI required)
p115client==0.0.9.3.7   # 锁定版本：不锁会在重建镜像时被动升级，新版曾移除 __init__ 的 check_for_relogin 参数导致启动崩溃
# p115client 0.0.9.3.7 仍会导入 asynctools.to_list；新版 python-asynctools 已移除该符号。
# 必须显式锁定传递依赖，否则 Docker 的无缓存构建会成功安装、运行时却在导入阶段崩溃。
python-asynctools==0.1.3.4
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.66.0
colorama>=0.4.6
watchdog>=3.0.0
python-telegram-bot>=20.0
psutil>=5.9.0
ruamel.yaml>=0.17.0

# GUI dependencies (optional, local use only)
# PyQt6>=6.6.0
# PyQt6-Qt6>=6.6.0
